# 🎨 Logo → Code

AI-powered logo generation tool. Drop in inspiration images, describe your brand, and get SVG logo variations generated by Claude.

## Features

- **Drag & drop inspiration logos** — up to 8 images as a moodboard
- **Brand context** — name, industry, style vibes, color preferences
- **AI-generated SVG logos** — multiple unique variations (wordmark, icon, monogram, abstract, etc.)
- **Multi-size preview** — see each logo at 32px, 64px, 200px, and 400px
- **Light/dark background toggle** per card
- **Export options** — Copy SVG, download .svg, copy as React JSX component, download PNG (1024px)
- **Regenerate & refine** — regenerate individual variations or refine with natural language

## Usage

1. Open `index.html` in a browser (or visit the [GitHub Pages site](https://0xmikey-ooze.github.io/logo-to-code/))
2. Click **API Key** and enter your Anthropic API key
3. Add inspiration images, fill in brand details, select styles
4. Hit **Generate Logos**
5. Export, refine, or regenerate as needed

## Tech

- Single HTML file, zero dependencies, no build step
- Direct browser fetch to Anthropic API (Claude Opus)
- API key stored in localStorage
- Dark theme with Inter + JetBrains Mono

## License

MIT
